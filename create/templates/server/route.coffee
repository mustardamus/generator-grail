module.exports = (config, helpers, io, models) ->
  @get '/{{pluralized}}', (req, res, next) ->
  @post '/{{pluralized}}', (req, res, next) ->
  @put '/{{pluralized}}', (req, res, next) ->
  @delete '/{{pluralized}}', (req, res, next) ->
