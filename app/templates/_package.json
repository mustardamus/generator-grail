{
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "author": "{{author}}",
  "license": "{{license}}",
  "main": "client/index.coffee",
  "scripts": {
    "start": "forever start -c coffee --watch --watchDirectory=\"server\" server/index.coffee",
    "stop": "forever stop server/index.coffee",
    "test": "gulp test"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "devDependencies": {
  },
  "dependencies": {
  }
}
