{
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "author": "{{author}}",
  "license": "{{license}}",
  "main": "client/index.coffee",
  "scripts": {
    "start": "forever -c coffee -f -m 3 -w --watch --watchDirectory=\"server\" server/index.coffee",
    "stop": "",
    "test": "gulp test"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "devDependencies": {
  },
  "dependencies": {
  }
}
